{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "bash -lc 'if [ -f frontend/package-lock.json ]; then cd frontend && npm ci --include=dev; else cd frontend && npm install; fi'",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://YOUR_BACKEND_HOST/:path*"
    }
  ]
}
