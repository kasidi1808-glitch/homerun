{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "bash -lc 'if [ -f scripts/infra/vercel-frontend-deploy.sh ]; then bash scripts/infra/vercel-frontend-deploy.sh; elif [ -f ../scripts/infra/vercel-frontend-deploy.sh ]; then bash ../scripts/infra/vercel-frontend-deploy.sh; else echo \"Missing scripts/infra/vercel-frontend-deploy.sh in current or parent directory\" >&2; exit 127; fi'",
  "buildCommand": "echo \"Build completed in install step\"",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://YOUR_BACKEND_HOST/:path*"
    }
  ]
}
